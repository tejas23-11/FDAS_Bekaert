# Capture / CV (Member 2)
opencv-python-headless==4.10.*
numpy==1.26.*

# OCR (Member 2)
paddleocr
paddlepaddle
pytesseract==0.3.*         # fallback OCR engine if paddleocr is not available

# Backend / storage (Member 3)
# sqlite3 is in the Python standard library — no package needed
# psycopg2-binary==2.9.*  # uncomment if moving to PostgreSQL for multi-site

# Notifications (Member 4) -- GSM-only, confirmed decision (no cloud gateway)
pyserial==3.5                # AT-command serial connection to the SIM7600 --
                              # used for both SMS (notify/sms_gateway.py) and
                              # the blank voice call (notify/voice_call.py)

# Config
pyyaml==6.*

# Watchdog / reliability (Member 5)
psutil==6.*

# Testing (Member 6)
pytest==8.*

# UI — Operator Dashboard (step 14)
flask==3.1.*
openpyxl==3.1.*
